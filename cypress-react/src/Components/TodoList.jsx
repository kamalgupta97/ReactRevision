import React from 'react';

export const TodoList = ({ title, completed }) => {
    return <li>
        <input type="checkbox" name="" id="" />
        <label htmlFor="checkbox">{title}</label>
    </li>;
};
